# Copy to .env and fill in real values. .env is gitignored — never commit secrets.

# Anthropic API (needed from CP3 onward)
ANTHROPIC_API_KEY=

# Search API (decide provider at CP2 — Brave / Tavily / Serper)
SEARCH_API_KEY=
